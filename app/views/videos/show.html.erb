<!DOCTYPE html>
<html>
<body>
    <div class="center jumbotron">
        <h1><%= @video.title %></h1>
        <h2><%= @video.description %></h2>
        <video id="req-video" class="player-pane" src="<%= rails_blob_path(@video.file, disposition: 'attachment') %>"
            playsinline autoplay controls>
        </video>
    </div>
    <%= link_to image_tag("rails.svg", alt: "Rails logo", width: "200"), "//rubyonrails.org/" %>
</body>
</html>
<% @video.file_path %>
